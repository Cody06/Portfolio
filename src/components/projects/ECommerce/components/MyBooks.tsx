import SectionHeader from './SectionHeader';

export default function MyBooks() {
    return (
        <section className="mx-4">
            <SectionHeader title="My books collection" />
            {/* TODO: Pass a list of books */}
        </section>
    );
}
